# Vendor Directory

Place pre-downloaded Python wheels in this directory for offline installation.
For example download `Django-5.2.2-py3-none-any.whl` from PyPI on a machine with
internet access and copy it here before running `scripts/setup_test_env.sh`.
